can you see the wz-helper app and its references? and the overwolf docs in root references

i want you to make a new overwolf app called "Warzone Helper" in the wz-helper dir, with a native plugin that can also be run standalone via dllrun or with a accompanying exe in console mode that does a few things, it should monitor changes in warzones logs/cache dirs so it can dispatch events for these, it should monitor network connections of cod.exe to dispatch events like "GAME_SERVER_CONNECTED", "GAME_SERVER_DISCONNECTED" with maxmind local country resolution and server pings, etc in the event and figure out which connections are actual game server and which arent. it should also monitor cod status api (a bit like) P:\Python\red-discordbot-cogs\codstatus and should throw events when status changes and it should also have the ability (either through using existing overlay capture apis or manual 1 second screenshotting to give events based on whats in the game screen (like when a match starts, when im deployed, when my health changes, when im dead, when the lobbyid (the number in the bottom left changes), etc, etc